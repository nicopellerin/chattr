import * as React from "react"
import styled from "styled-components"

const ChatCommands = () => {
  return <Wrapper>ChatCommands</Wrapper>
}

export default ChatCommands

// Styles
const Wrapper = styled.div`
  background: #111;
  height: 100%;
  padding: 2rem;
`
